name: Audit

on:
  schedule:
    - cron: 56 11 * * THU

jobs:
  pnpm-audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/setup@v1
        with:
          version: 11
          runtime: node@24
          cache: true

      - run: pnpm audit
